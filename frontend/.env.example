# Copia este archivo a `.env.local` para desarrollo
# o configura las variables en el dashboard de Vercel para producción.

# URL del backend (Render en producción, localhost en dev).
# Sin barra al final.
VITE_API_URL=http://127.0.0.1:8000
